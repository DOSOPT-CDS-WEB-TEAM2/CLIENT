import { CommonButton, CommonButtonContainer } from '../../Common/buttons';

const InOutComeInfoGraph = () => {
  return (
    <>
      <CommonButtonContainer>
        <CommonButton>매분기</CommonButton>
        <CommonButton>연간</CommonButton>
      </CommonButtonContainer>
    </>
  );
};

export default InOutComeInfoGraph;
